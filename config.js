module.exports = {
  token: procces.env.TOKEN || "BOT_TOKEN",
  guildId: procces.env.SVID || "GUILD_ID",
  verifyRole: procces.env.VFY_ROLE || "VERIFY_ROLE",
  verifyChannel: procces.env.VFY_CH || "VERIFY_CHANNEL",
};
